export class User {
    name: string;
    email: string;
    password: any;
   
    constructor(name: string, email:string, password: any, ) {
     this.name = name;
     this.email = email;
     this.password = this.password;
    }
   }
   